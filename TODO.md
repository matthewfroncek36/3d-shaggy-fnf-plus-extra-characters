# TODO

- [ ] Fix `source/mobile/psychlua/Functions.hx` to re-enable Lua touchpad callbacks:
  - [ ] Restore `addTouchPad` implementation
  - [ ] Restore `removeTouchPad` implementation
  - [ ] Restore `addTouchPadCamera` implementation
  - [ ] Restore `touchPadJustPressed/Pressed/JustReleased/Released` implementations
- [ ] Compile/build to verify no Haxe errors.

